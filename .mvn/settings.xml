<settings>
    <servers>
        <server>
            <id>anypoint-exchange-v3</id>
            <username>${secrets.MULE_NEXUS_USERNAME}</username>
            <password>${secrets.MULE_NEXUS_PASSWORD}</password>
        </server>
        <!-- Add other server configurations as needed -->
    </servers>
</settings>
